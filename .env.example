# Copie para .env.local. Nunca versione a chave real.
APP_ORIGIN=http://localhost:3000
# test: qualquer e-mail, acesso por link. email: exige confirmação por e-mail.
ACCESS_MODE=test
# allowlist: somente a lista abaixo cria planos. open: todo e-mail confirmado cria.
# Ausente ou vazio mantém allowlist; use exatamente allowlist ou open.
APP_AUTHOR_MODE=allowlist
# Lista separada por vírgulas usada no modo allowlist.
APP_AUTHOR_EMAILS=autora@empresa.com,autor@empresa.com
# Configuração legada: continua habilitando este e-mail e preserva seu nome.
APP_OWNER_EMAIL=voce@empresa.com
APP_OWNER_NAME=Seu nome
# Tetos totais de escrita do piloto. Use somente inteiros positivos.
MAX_OWNED_DOCUMENTS=100
MAX_COMMENTS_PER_DOCUMENT=500
MAX_ACTIVE_SHARES_PER_DOCUMENT=100
# Serviço de envio; use um remetente em domínio verificado.
RESEND_API_KEY=
MAIL_FROM=
# O runner Node usa 30 segundos; o Worker usa o cron versionado de 1 minuto.
NOTIFICATION_DRAIN_LIMIT=10
NOTIFICATION_DRAIN_INTERVAL_MS=30000
NOTIFICATION_LEASE_SECONDS=60
NOTIFICATION_RETRY_BASE_SECONDS=30
